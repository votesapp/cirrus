// helpers etc for voteCreate template

if (Meteor.isClient) {

  Template.voteCreate.helpers({

  });

  Template.voteCreate.events({

    "submit #createVote" : function () {
      // create the vote
    }
  });

};